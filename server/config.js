const headless = true;
const record = false;

const browsers = {
    //snai: 'webkit',
};

const plugins = {
    /*snai: {
        balances: 'puppeteer',
        activeBets: 'puppeteer',
        dailySpin: 'playwright'
    }*/
}

module.exports = { headless, record, browsers, plugins };